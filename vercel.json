{
  "builds": [{ "src": "webpack-build-command", "use": "@vercel/static-build" }],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "handle": "filesystem" },
    { "src": "(/favicon.ico|/robots.txt)", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
